// const toggles = document.querySelectorAll(".faq-toggle")

// console.log("yjhv", toggles)

// toggles.forEach(toggle => {
//     toggle.addEventListener("click", () => {
//         toggle.parentNode.classList.toggle("active")
//     })
// })

const toggles = document.querySelectorAll(".faq-toggle")
console.log("jeckn", toggles)


toggles.forEach(toggle => {
    toggle.addEventListener("click", () => {
        toggle.parentNode.classList.toggle("active")
    })
})